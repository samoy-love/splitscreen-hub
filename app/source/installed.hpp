#pragma once

#include <set>
#include <string>

/// Список Title ID игр, установленных на консоли.
///
/// Это то, чего нет в eShop: приложение может сказать «из подходящего у вас уже
/// установлено 12 игр». Сопоставление идёт по Title ID из каталога — он
/// заполнен у всех игр.
namespace installed
{

/// Читает список установленных приложений через ns:am. На не-Switch сборке
/// (отладка на десктопе) возвращает пустое множество.
std::set<std::string> titleIds();

}  // namespace installed
