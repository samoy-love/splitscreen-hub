#pragma once

#include <functional>
#include <string>

/// Выбор папки для игры — общий для каталога и карточки.
///
/// Разложить десяток игр по папкам должно быть можно из каталога, не открывая
/// десяток карточек. Логика одна и та же, поэтому живёт в одном месте.
namespace folders
{

/// Показывает список папок с отметками о том, где игра уже лежит, и позволяет
/// завести новую прямо отсюда. onChanged зовётся после изменения — экран должен
/// обновить свои отметки.
void pick(const std::string& nsuid, std::function<void()> onChanged);

}  // namespace folders
