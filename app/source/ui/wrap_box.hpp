#pragma once

#include <borealis.hpp>

/// Контейнер с переносом строк.
///
/// Раскладку borealis считает через yoga, а та перенос умеет — но borealis не
/// выставляет его в XML, и атрибут «wrap» на brls:Box валит разбор всего файла.
/// Вместо правки библиотеки заводим свой тег: обновлять форк borealis это не
/// мешает.
///
/// Нужен там, где число подписей заранее неизвестно: строка фильтров в каталоге,
/// жанры в карточке игры, кнопки в менеджере кеша.
class WrapBox : public brls::Box
{
  public:
    WrapBox();

    static brls::View* create();
};
